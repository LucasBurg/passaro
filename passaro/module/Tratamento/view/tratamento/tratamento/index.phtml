<?php
$title = 'Lista de Tratamentos';
$this->headTitle($title);
?>
<div class="row">
    <div class="col-md-12">
        <h1><?=$title?></h1>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <?php
        $urlAdd = $this->url('tratamentos', ['action' => 'add']);
        ?>
        <a href="<?=$urlAdd?>" class="btn btn-default">Adicionar</a>
    </div>
</div>
<div class="row">
    <div class="col-md-12">
        <table class="table">
            <thead>
                <tr>
                    <th class="col-md-1 text-center">ID</th>
                    <th>Nome</th>
                    <th class="col-md-2 text-center">#</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($tratamentos as $tratamento) : ?>
                <tr>
                    <td class="col-md-1 text-center"><?=$tratamento->id?></td>
                    <td><?=$tratamento->nome?></td>
                    <td class="col-md-2 text-center">
                        <?php
                        $urlEdit = $this->url('tratamentos', 
                                ['action' => 'edit', 'id' => $tratamento->id]);
                        $urlDelete = $this->url('tratamentos', 
                                ['action' => 'delete', 'id' => $tratamento->id]);
                        ?>
                        <a href="<?=$urlEdit?>" class="btn btn-primary">Editar</a>
                        <a href="<?=$urlDelete?>" class="btn btn-danger">Deletar</a>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>
